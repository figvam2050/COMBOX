# PCS-RS485: діагностика COMBOX з Mac

## Призначення

Цей режим потрібен для стендової перевірки без інвертора:

```text
Vision BMS RS485 -> COMBOX -> PCS-RS485 -> USB-RS485 -> Mac
```

COMBOX самостійно опитує Vision BMS. Mac не повинен надсилати запити в BMS-485; він читає тільки діагностичний порт PCS-RS485. Це виключає конфлікт двох Modbus-master на лінії акумулятора.

PCS-RS485 не емулює протокол Deye і не передає дані сам по собі. Mac має надіслати Modbus-запит, після чого COMBOX повертає останню валідну телеметрію BMS.

## Підключення

1. Підключіть Vision BMS до роз'єму COMBOX `BMS-485` як звичайно.
2. Підключіть USB-RS485 адаптер Mac до роз'єму COMBOX `PCS-485`.
3. З'єднайте лише `A -> A`, `B -> B` та, якщо він доступний, `GND -> GND`.
4. Не подавайте 5 V або інше живлення з USB-RS485 адаптера в роз'єм COMBOX.
5. Якщо немає відповіді, поміняйте місцями тільки `A` та `B` на стороні адаптера.

## ModMaster на macOS

Налаштування:

| Поле | Значення |
| --- | --- |
| Mode | Modbus RTU (serial) |
| Port | Ваш `/dev/cu.usbserial-*` |
| Baud rate | `9600` |
| Data bits / parity / stop bits | `8 / None / 1` |
| Slave address | `1` |
| Data table | Holding Registers (function `0x03`) |
| Register(s) | `1` |
| Count | `39` |
| Start reference base 0 | Вимкнено |

За цих параметрів Mac надсилає:

```text
01 03 00 00 00 27 05 D0
```

Валідна відповідь починається з `01 03 4E`. `LED5` на COMBOX коротко спалахує після кожної валідної відповіді Mac.

## Дані регістрів

`0x0000..0x0026` повторюють розібрані дані Vision BMS у тому самому порядку, що й запит до батареї:

| PDU-регістр | Дані |
| --- | --- |
| `0x0000` | Напруга пака, 0.01 V |
| `0x0001` | Струм пака, signed, 0.01 A |
| `0x0002..0x0011` | Напруги 16 комірок, mV |
| `0x0012..0x0014` | Температури PCB / середня / максимальна, C |
| `0x0015` | Залишкова ємність, Ah |
| `0x0016` | Ліміт струму заряду BMS, A |
| `0x0017..0x0018` | SOH / SOC, % |
| `0x0019..0x001B` | Status / Warning / Protection |
| `0x001C..0x0026` | Нуль, резерв для повної 39-регістрової відповіді |

Додаткові діагностичні регістри:

| PDU-регістр | Дані |
| --- | --- |
| `0x0100` | `0xC0B0`: ознака діагностичної прошивки |
| `0x0101` | Поточна Modbus-адреса BMS, вибрана DIP COMBOX |
| `0x0102` | `1` якщо телеметрія BMS валідна, `0` якщо її немає або вона застаріла |
| `0x0103` | Вік останніх даних у десятих секунди; `0xFFFF` якщо даних немає |

Для читання цих чотирьох регістрів у SerialTool, увімкнувши HEX, надішліть:

```text
01 03 01 00 00 04 45 F5
```

У ModMaster вимкніть `Start reference base 0`, встановіть `Register(s) = 257` та `Count = 4`.

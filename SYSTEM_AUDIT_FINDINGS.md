# Комплексний аудит та технічний аналіз системи COMBOX (Vision BMS -> Deye Inverter)

**Дата аналізу:** 4 вересня 2026 р.  
**Цільовий контролер:** GigaDevice GD32F305RCT6 (плата Felicity "BMS COM BOX")  
**Акумулятор:** Vision V-LFP48100 (48V 100Ah, 15S LiFePO4, BMS: `M-BMS-15S100A-Vis-V1.0`)  
**Інвертор:** Deye Hybrid Low-Voltage (протокол літієвої батареї: `00` / Pylontech CAN 500 kbps)  
**Аналізовані матеріали:**
* Вихідний код: `src/main.cpp`, `platformio.ini`, `GD32F305RC_COMBOX.ld`
* Документація проекту: `README.md`, `PORT_MAPPING.md`, `FACTORY_PIN_ANALYSIS.md`, `FLASHING_INSTRUCTIONS.md`
* Заводські специфікації: *Vision V-LFP48100 Battery BMS Specification* (`M-BMS-15S100A-Vis-V1.0`), *MODbus Communication Protocol between UPS(SMPS) and BMS V01.01*, заводський дамп прошивки `combobox_bkp_40000.bin`.

---

## Статус виправлень, 8 вересня 2026 р.

Початковий текст аудиту нижче збережений як історичний запис від 4 вересня. Актуальна standalone-прошивка та документація мають такі зміни:

- CAN-бітрейт обчислюється з фактичного `PCLK1`; для 36 МГц використовується точний режим 500 кбіт/с.
- Струм Vision передається в `0x356` без інверсії знаку: заряд `+`, розряд `-`.
- Робоча Modbus-адреса поточної Vision BMS зафіксована як `0x04` на підставі живого читання `04 03 00 00 00 27 05 85`.
- DIP `0` вмикає один BMS за адресою `0x04`; інші положення запускають сканування `0x01..0x04`.
- CAN реалізовано за профілем `LiBms: Deye`: `0x351`, `0x355`, `0x356`, `0x35C`, усі з `DLC=8`, раз на 500 мс.
- При втраті BMS не використовуються застарілі дані: передаються нульові ліміти та нульові вимірювання.
- Кадр `0x359` прибрано з базового обміну, бо відповідність бітів захистів Vision до Deye не підтверджена. `0x35C` не встановлює неперевірені біти `6` і `7` та не вмикає force-charge автоматично.

Актуальний опис кадрів: `DEYE_CAN_PROTOCOL.md`.

---

## 1. Резюме аудиту

Проведено поглиблений аналіз математичних розрахунків, налаштувань переферії мікроконтролера, відповідності протоколів зв'язку та фізичних підключень.
Виявлено **3 критичні помилки**, які призведуть до повної відсутності зв'язку з інвертором або до некоректних аварійних режимів роботи, а також **3 протокольні спрощення**.
Водночас підтверджено **100% точність карти регістрів Modbus**, порогів напруг 15S LiFePO4 та апаратної розпіновки плати.

---

## 2. КРИТИЧНІ ПОМИЛКИ ТА АПАРАТНІ НЕВІДПОВІДНОСТІ

### 🔴 2.1. Критична помилка швидкості шини CAN (562.5 кбіт/с замість 500.0 кбіт/с)

#### Локалізація в коді:
Файл [`src/main.cpp`](src/main.cpp#L257-L264):
```cpp
// APB1 = 32 MHz with standard Arduino STM32F1 HSI PLL (64MHz SYSCLK / 2 = 32MHz APB1)
// 32 MHz / (Prescaler 4 * 16 TQ) = 500 kbit/s
// SJW = 2 TQ (1U << 24)
// TS2 = 4 TQ (3U << 20)
// TS1 = 11 TQ (10U << 16)
// BRP = 4 (prescaler = 3U + 1)
CAN1->BTR = (1U << 24) | (3U << 20) | (10U << 16) | 3U;
```

#### Суть проблеми:
1. Розрахунок у коментарі та регістрі `BTR` базується на застарілому припущенні неофіційного ядра Maple (Roger Clark), де частота внутрішнього генератора HSI подвоювалася до 64 МГц, а шина $\text{APB1} = 32\text{ МГц}$.
2. У поточному проекті використовується офіційне ядро STMicroelectronics (`ststm32` / `STM32Duino`) з налаштуванням у `platformio.ini`: `-D HSE_VALUE=8000000U`.
3. Файл ініціалізації тактування плати (`variant_BLUEBUTTON_F103RxT.cpp`) запускає PLL від зовнішнього кварцу 8 МГц з множником 9:
   $$\text{SYSCLK} = 8\text{ МГц} \times 9 = 72\text{ МГц}$$
   Дільник шини APB1 дорівнює 2, тому реальна частота шини:
   $$\mathbf{f_{PCLK1} = \frac{72\text{ МГц}}{2} = 36\text{ МГц}}$$
4. При $\text{f_{PCLK1}} = 36\text{ МГц}$ і жорстко зашитому дільнику ($BRP=4$, $16\text{ TQ}$):
   $$\text{Actual Baud Rate} = \frac{36{,}000{,}000}{4 \times (1 + 11 + 4)} = \frac{36{,}000{,}000}{64} = \mathbf{562{,}500\text{ bit/s}}\ (562.5\text{ кбіт/с})$$
5. Похибка становить **$+12.5\%$** від номінальних $500\text{ кбіт/с}$. За стандартом CAN bus гранично допустима похибка бітрейту становить не більше $\pm 1.58\%$.

#### Наслідок:
Інвертор Deye працює суворо на $500\text{ кбіт/с}$. При різниці у $12.5\%$ контролер CAN Deye фіксуватиме фазові помилки (*Bit Stuff Error*, *CRC Error*), не підтверджуватиме прийом кадрів (відсутність ACK), переведе шину COMBOX у стан **Bus-Off**, а на екрані інвертора з'явиться аварія `F56 BMS Comm Fault`.

---

### 🔴 2.2. Інверсія знаку струму в кадрі CAN 0x356 (переплутано заряд і розряд)

#### Локалізація в коді:
Файл [`src/main.cpp`](src/main.cpp#L105) та [`src/main.cpp`](src/main.cpp#L536-L545):
```cpp
// CURRENT SIGN CONVERSION:
// Vision BMS: 10 mA, discharge > 0, charge < 0.
// Pylontech CAN 0x356: 0.1 A, charge > 0, discharge < 0.
// We divide by 10 and invert sign:
int32_t current01A32 = - (totalCurrentRaw / 10);
```

#### Суть проблеми:
1. У коді помилково припущено, що BMS Vision видає додатній струм при розряді та від'ємний при заряді.
2. В офіційній заводській інструкції виробника (*Vision V-LFP48100 BMS Specification*, Section 2):
   > *"The current value is shown as a positive when charging, a negative when discharging."*
   *(Струм позитивний при заряді, негативний при розряді).*
3. В офіційній таблиці Modbus (*MODbus Protocol V01.01*, Регістр `0001`):
   > *"0 ~ 32767: Charge, -32768 ~ 0: Discharge"*
4. У специфікації кадру Pylontech CAN `0x356` (Байти 2-3, signed 16-bit, 0.1 A):
   * Додатній струм ($> 0$) = **Заряд (Charge)**
   * Від'ємний струм ($< 0$) = **Розряд (Discharge)**

#### Наслідок:
Полярність струму у Vision BMS та Pylontech CAN **повністю збігається від природи**. Штучне інвертування знаку через оператор мінус (`-`) призводить до того, що:
* Коли інвертор **розряджає** батарею, Deye вважає, що вона **заряджається** величезним струмом.
* Коли інвертор **заряджає** батарею, Deye фіксує відтік енергії.
* Показники лічильника енергії та алгоритми керування зарядом інвертора повністю спотворюються.

---

### 🔴 2.3. Помилка розпіновки кабелю RS485 у документації `README.md`

#### Локалізація в документації:
Файл [`README.md`](README.md#L41):
```text
Кабель до АКБ: Pin 1 (A) -> Pin 6 COMBOX, Pin 3 (B) -> Pin 5 COMBOX, Pin 2 (GND) -> Pin 1 COMBOX.
```

#### Суть проблеми:
Цей рядок суперечить паспортній розпіновці роз'єму RJ45 на корпусі акумулятора Vision.
В офіційній специфікації Vision BMS (Section 7.2 *Communication*, Figure 7.1) цоколівка роз'єму RJ45 чітко визначена:

| Пін RJ45 Vision BMS | Сигнал за паспортом Vision | Призначення за помилковим описом `README.md` | Результат помилки |
| :---: | :---: | :---: | :--- |
| **Pin 1** | **RS485-B (T/R-)** | Вказано як "A" | Лінії даних переплутані |
| **Pin 2** | **RS485-A (T/R+)** | Вказано як "GND" | **Лінію даних А посаджено на землю!** |
| **Pin 3** | **GND** | Вказано як "B" | Землю підключено до диференційного входу В |
| Pin 6 | **GND** | — | Друга земля роз'єму |
| Pin 7 | **RS485-A (T/R+)** | — | Дублюючий вхід A |
| Pin 8 | **RS485-B (T/R-)** | — | Дублюючий вхід B |

#### Наслідок:
Якщо монтажник виготовить патч-корд відповідно до рядка 41 `README.md`, сигнальний пін **RS485-A (Pin 2)** буде з'єднаний із землею GND COMBOX, а земля батареї **(Pin 3)** піде на диференційну лінію B. Трансивер RS485 не зможе приймати чи передавати дані.

---

## 3. ПРОТОКОЛЬНІ НЕВІДПОВІДНОСТІ ТА СПРОЩЕННЯ

### 🟡 3.1. Спрощене мапування бітів захисту в кадрі CAN 0x359

#### Локалізація в коді:
Файл [`src/main.cpp`](src/main.cpp#L465-L474):
```cpp
void buildFrame359(uint16_t warning, uint16_t protection, uint8_t out[8]) {
  memset(out, 0, 8);
  if (protection != 0)
    out[0] = 0x01; // generic protection active
  if (warning != 0)
    out[2] = 0x01; // generic warning active
  out[4] = 0x01;   // module count
  out[6] = 'P';
  out[7] = 'N';
}
```

#### Суть проблеми:
* У стандарті Pylontech CAN кадр `0x359` містить побітове розшифрування статусів. Байт 0 має наступне призначення:
  * Біт 0: *Discharge High Current Protection* (струм розряду)
  * Біт 1: *Charge High Current Protection* (струм заряду)
  * Біт 2: *High Temperature Protection* (висока температура)
  * Біт 3: *Low Temperature Protection* (низька температура)
  * Біт 4: *Under Voltage Protection* (перерозряд комірки/пака)
  * Біт 5: *Over Voltage Protection* (перезаряд комірки/пака)
* Запис `out[0] = 0x01` встановлює лише біт 0 (*Discharge High Current*).
* Якщо на батареї спрацює захист від перегріву (High Temp) або перезаряду (Pack OV), Deye покаже аварію «Перевантаження за струмом розряду» замість реальної причини аварії.

---

### 🟡 3.2. Невикористаний («мертвий») код DIP-перемикачів COMBOX

#### Локалізація в коді:
Файл [`src/main.cpp`](src/main.cpp#L134-L143):
```cpp
uint8_t readFactoryDipAddress() {
  const uint32_t masks[] = {1UL << 15, 1UL << 14, 1UL << 13, 1UL << 12};
  const uint8_t weights[] = {8, 4, 2, 1};
  ...
}
```

#### Суть проблеми:
Функція опитування пінів `PB12..PB15` повністю описана, але ніколи не викликається ні в `setup()`, ні в `loop()`. Змінна вибору режиму не використовує результат зчитування DIP. У результаті положення фізичних перемикачів на корпусі COMBOX повністю ігнорується, і прошивка завжди працює з жорстко зашитою адресою `0x10`.

---

### 🟡 3.3. Поведінка при втраті зв'язку з АКБ (Hard Drop)

#### Локалізація в коді:
Файл [`src/main.cpp`](src/main.cpp#L528-L530):
```cpp
  if (validCount == 0)
    return;
```

#### Суть проблеми:
Якщо зв'язок RS485 з акумулятором втрачається (обрив дроту, вимкнення автомата АКБ), через 5 секунд (`BMS_STALE_MS`) телеметрія визнається невалідною, і COMBOX **повністю перестає надсилати CAN-кадри інвертору**.
Інвертор Deye після 5–10 секунд відсутності пакетів переходить у стан аварії `F56 BMS Comm Fault` із гучним звуковим сигналом.
*(Промислова альтернатива — продовжувати слати кадри з нульовими дозволеними струмами $CCL=0, DCL=0$ та встановленим бітом внутрішньої помилки BMS, щоб інвертор штатно зупинився).*

---

### 🟡 3.4. Відсутність прапорця примусового заряду (Force Charge) при розряді $\le 5\%$

У кадрі `0x35C` Pylontech байт 0 містить біт 5 (`0x20`), який відповідає за прапорець **Force Charge Request**. Коли батарея розряджається до критичних $5\%$ SOC, батарея вимагає увімкнути підзарядку від мережі або генератора. У поточній реалізації встановлюються лише біти 7 (`0x80` Charge Enable) та 6 (`0x40` Discharge Enable).

---

## 4. ЩО ПЕРЕВІРЕНО І ВРАХОВАНО ПОВНІСТЮ ПРАВИЛЬНО ✅

1. **Карта регістрів Modbus Vision:**
   * Запит `10 03 00 00 00 27 06 91` опитує 39 регістрів починаючи з `0x0000`.
   * Зсуви всіх байтів у буфері відповіді (довжина 83 байти) розраховані абсолютно точно:
     * Байти 3-4: Напруга пака (масштаб 10 мВ).
     * Байти 5-6: Струм пака (масштаб 10 мА).
     * Байти 7..38: 16 комірок (для 15S комірка 16 = 0 мВ).
     * Байти 39-44: Температури (PCB, Avg, Max) у °C.
     * Байти 45-52: Залишкова ємність, максимальний струм заряду, SOH, SOC.
     * Байти 53-58: Регістри Status, Warning, Protection.
2. **Безпечні ліміти напруг:**
   * `CHARGE_VOLTAGE_LIMIT_01V = 540` ($54.0\text{ В}$) — ідеально відповідає паспортному *Rated charging voltage* ($54\text{ В}$) для 15S LiFePO4 ($3.60\text{ В}$/комірка).
   * `DISCHARGE_VOLTAGE_LIMIT_01V = 430` ($43.0\text{ В}$) — безпечна відсічка, що спрацьовує вище порогу апаратного вимкнення BMS ($42.0\text{ В}$).
3. **Формування кадрів CAN для Deye:**
   * `0x351`: CVL ($0.1\text{ В}$), CCL ($0.1\text{ А}$), DCL ($0.1\text{ А}$), DVL ($0.1\text{ В}$) у форматі Little-Endian.
   * `0x355`: SOC ($1\%$), SOH ($1\%$) у форматі Little-Endian.
   * `0x35E`: Рядок ідентифікатора виробника `"PYLON   "`.
4. **Апаратна розпіновка плати COMBOX:**
   * Підтверджено трасування за заводським дампом: `PB0` (LED3 Run), `PB1` (LED4 BMS RS485), `PC5` (LED6 PCS CAN).
   * Службові рівні `PC3 = LOW`, `PC4 = HIGH` виставлені згідно з заводським реверсом.
   * Трансивер CAN на пінах `PB8/PB9` з частковим ремапом (`AFIO_MAPR = 0x00004000`).
   * Трансивер BMS RS485 на пінах `PA9/PA10` (USART1).

---

## 5. РЕКОМЕНДОВАНИЙ ПЛАН ДІЙ ДЛЯ УСУНЕННЯ ПОМИЛОК

1. **Виправити розрахунок бітрейту CAN:**
   Розраховувати `BTR` виходячи з реальної частоти $\text{PCLK1} = 36\text{ МГц}$:
   * При $\text{PCLK1} = 36\text{ МГц}$, для швидкості $500\text{ кбіт/с}$ на один біт припадає $72$ такти.
   * Оптимальне розбиття: Prescaler = 4, $18\text{ TQ}$ (Sync = 1, TS1 = 13, TS2 = 4, SJW = 2), або Prescaler = 6, $12\text{ TQ}$ (Sync = 1, TS1 = 8, TS2 = 3, SJW = 2).
   * Або використати функцію `HAL_RCC_GetPCLK1Freq()` для автоматичного розрахунку.
2. **Виправити знак струму:**
   Прибрати унарний мінус: `int32_t current01A32 = totalCurrentRaw / 10;`.
3. **Оновити документацію кабелю RS485 у `README.md`:**
   Чітко зафіксувати, що на роз'ємі Vision BMS RJ45: **Pin 1 — це B(-)**, **Pin 2 — це A(+)**, а **Pin 3 — це GND**.
4. **Покращити бітове мапування аварій у кадрі `0x359`:**
   Транслювати окремі прапорці `PROT_PACK_OV`, `PROT_PACK_UV`, `PROT_CHARGE_OT` тощо у відповідні позиції бітів стандарту Pylontech.

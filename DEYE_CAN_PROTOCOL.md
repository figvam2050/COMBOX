# Deye LV-CAN: реалізація COMBOX

Цей документ описує CAN-обмін, який реалізовано в `src/main.cpp` для зв'язки Vision V-LFP48100 -> COMBOX -> Deye.

## Підстава

- У бінарному образі інвертора `deye/MCU1-SG-Ver0000-C366-CK.bin` для профілю `LiBms: Deye` знайдено обробку стандартних CAN ID `0x351`, `0x355`, `0x356`, `0x35A` і `0x35C`.
- Режим CAN: 500 кбіт/с, standard CAN 2.0A, 11-бітні ідентифікатори, дані по 8 байтів, little-endian для 16-бітних чисел.
- Незалежна специфікація: [Deye LV CAN Communication Protocol](https://www.i4wifi.cz/uploads/faq/2003_1_deye-lv-can-communication-protocol-2-.pdf).

## Кадри, які передає COMBOX

| ID | Період | Дані `byte[0]..byte[7]` |
| --- | --- | --- |
| `0x351` | 500 мс | `CVL u16 LE` x 0.1 V, `CCL u16 LE` x 0.1 A, `DCL u16 LE` x 0.1 A, `DVL u16 LE` x 0.1 V |
| `0x355` | 500 мс | `SOC u16 LE` у %, `SOH u16 LE` у %, `00 00 00 00` |
| `0x356` | 500 мс | напруга пака `u16 LE` x 0.01 V, струм `i16 LE` x 0.1 A, температура `i16 LE` x 0.1 C, `00 00` |
| `0x35C` | 500 мс | Усі байти `00` у нормальному режимі. Тільки `byte[0] bit 5` (`0x20`) має підтверджене значення: повний/примусовий заряд. COMBOX не встановлює його без окремого підтвердженого сигналу від BMS. |

Усі кадри мають `DLC=8`, зокрема `0x355` і `0x356`. Не можна надсилати вкорочені `DLC=4` або `DLC=6`.

## Дані від інвертора

Інвертор може періодично передавати `0x305` з вісьмома нульовими байтами. COMBOX приймає цей запит і одразу передає звичайний набір чотирьох кадрів. Передача також відбувається циклічно незалежно від `0x305`, тому обмін не залежить від конкретної версії Deye.

## Перетворення Vision Modbus -> CAN

| Дані Vision | Регістр | Перетворення в Deye |
| --- | --- | --- |
| Напруга пака | `0x0000`, 0.01 V | Без зміни в `0x356` |
| Струм пака | `0x0001`, 0.01 A, signed | Ділення на 10 для `0x356` (0.1 A), знак не інвертується: заряд `+`, розряд `-` |
| Температура середня | `0x0019`, C | Множення на 10 для `0x356` |
| Максимальний струм заряду | `0x0022`, A | Обмежується безпечним максимумом і множиться на 10 для CCL |
| SOC, SOH | `0x0024`, `0x0023`, % | Без зміни в `0x355` |
| Захисти Vision | `0x0025..0x0027` | Нульовий CCL або DCL при відповідному захисті; неперевірені аварійні CAN-біти не передаються |

## Безпечний стан при втраті BMS

Після 5 с без валідної відповіді Modbus COMBOX не використовує останні значення. Вона продовжує відправляти чотири CAN-кадри, але всі напруги, SOC, SOH, струми та ліміти стають нульовими. Це забороняє заряд і розряд до відновлення зв'язку з BMS.

## Кадри, які не реалізовано навмисно

- `0x35A`: Deye-приймальник допускає ID, але в дослідженій гілці профілю не використовує його для базової роботи.
- `0x359`: кадр аварій Pylontech. Без повністю підтвердженої відповідності бітів Vision -> Deye його передача може повідомити інвертору хибну причину аварії.
- `0x35E`: ідентифікатор виробника. Він не обробляється як обов'язковий кадр у профілі `LiBms: Deye` з проаналізованого MCU1.
- Пропрієтарні 29-бітні стартові кадри, знайдені в MCU1. Для Vision BMS вони не потрібні на етапі базового LV-CAN, а їхній формат має бути підтверджений записом реальної CAN-шини перед реалізацією.
